<%= kramdown('##' + @article.title)%>
<%= kramdown('---')%>
<%= @article.content %>
<br>
<br>
<%= link_to "Edit", edit_article_path(@article) %>
<%= link_to "Delete", article_path(@article),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
      <br>
<%= link_to "Go back to the list of articles", articles_path  %>
